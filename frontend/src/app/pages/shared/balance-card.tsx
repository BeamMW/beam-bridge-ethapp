import React from 'react';
import { styled } from '@linaria/react';

const CardStyled = styled.div`

`;

const Card: React.FC = ({
  children,
  ...rest
}) => (
  <CardStyled>
    
  </CardStyled>
);

export default Card;
